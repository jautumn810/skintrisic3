/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone',
  // If you're using static HTML files, you might want to use 'export'
  // output: 'export',
}

module.exports = nextConfig

